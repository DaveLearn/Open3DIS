[build-system]
requires = ["setuptools<70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "yolo-world"
version = "0.1.0"
description = "Dummy project to bypass uv resolution"
dependencies = []
requires-python = ">=3.7"
